#include <stdlib.h>
#include <stdio.h>
#include "linklist.h"

void init(list* linklist, int len)
{
	linklist = (list* ) malloc(sizeof(list)*len);
}

void input(list* linklisti, int len)
{
	int i;
	for (i = 0; i < len; i++) {
		
	}
}
